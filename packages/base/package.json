{
  "name": "@coong/base",
  "version": "0.0.1",
  "description": "Shared functions, logics for Coong wallet",
  "scripts": {
    "build": "rm -rf ./dist && tsc --build"
  },
  "main": "src/index.ts",
  "dependencies": {
    "@coong/keyring": "*"
  }
}
